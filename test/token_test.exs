defmodule TokenTest do
  use ExUnit.Case
  doctest Token
  
  # test "get token id" do
  #   assert Token.get_token_id(:LEFT_PAREN) == {:ok, 0}
  #   assert Token.get_token_id(:RIGHT_PAREN) == {:ok, 1}
  #   assert Token.get_token_id(:EQUAL) == {:ok, 13}
  # end

end
